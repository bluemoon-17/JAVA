[참조타입]
1. 객체의 번지 참조
2. 배열, 열거, 클래스, 인터페이스
3. 기본 타입 - 변수 값 자체 저장
   참조 타입 - 객체가 생성된 메모리 번지 저장

[메소드, 힙, 스택 영역]
1. JVM - 운영체제서 할당받은 메모리 영역을 메소드, 힙, 스택 영역으로 구분 후 사용
2. 메소드 : 바이트 코드 파일을 읽는 내용이 저장
3. 힙 : 객체 생성 영역, 객체 번지 - 메소트, 스택의 상수와 변수서 참조
4. 스택 : 메소드 호출시 생성되는 프레임 저장

[==, != 연산자]
1. 객체 번지 비교 - 변수의 값이 같은지 아닌지 조사
2. 번지가 == 동일 객체 참조, =! 다른 객체 참조

[NULL값]
1. 참조 타입 변수는 아직 번지 저장 X
2. 초기값 사용 가능, null로 초기화된 참조변수는 스택 영역서 생성

[NullPointerException]
1. 변수가 null - 객체의 데이터, 메소드 사용시 발생하는 예외
2. 참조 변수가 객체를 정확히 참조하도록 번지를 대입해야 함

[String 타입]
1. 문자열 String 객체로 생성
2. 문자열 리터럴 동일시 String 객체 공유

[문자열 추출]
1. charAt() 
ex) 이정후 안타
    0 1 2 3 4 5

[문자열 길이]
1. length()
ex) 에스파 카리나
    0 1 2 34 56
예시의 길이는 7

[문자열 대체]
1. replace() - 기존 문자열은 두고 새 문자열 리턴

[문자열 잘라내기]
1. substring()

[문자열 찾기]
1. indexOf()

[문자열 분리]
1. split() - 구분자 있는 여러개의 문자열 분리

[배열]
1. 연속된 공간에 값 나열, 각 값에 인덱스 부여
2. 인덱스 - []와 함께 사용, 각 항목의 값을 읽거나 저장

[베열 변수 선언]
1. 두 가지 형태 (1. 관례적 표기)
2. 배열 - 힙 영역에 생성, 배열 변수 - 힙영역에 저장
3. 참조할 배열 X - 배열 변수 null 초기화 가능

[값 목록으로 배열 생성]
1. 배열에 저장될 값의 목록 존재 - 배열생성 가능
-> 타입[] 변수 = [값0, 값1, 값2, .....];
2. 배열 변수 선언 시점 != 값 목록 대입 시점 new타입을 중괄호 앞에 작성
   타입 - 배열 변수 선언시 사용한 타입과 동일하게 지정

[new연산자로 배열 생성]
1. 값의 목록은 없지만 향후 값들을 저장할 목적으로 배열 미리 생성
-> 타입[] 변수 = new 타입(길이);
2. new 연산자로 배열을 처음 생성 - 배열 항목 기본값으로 초기화

[배열 길이]
1. 배열의 저장할 수 있는 항목 수
2. 배열의 길이 얻기 - 도트(.)연산자 사용해 배열의 length필드 읽기
-> 배열변수.length;
3. 배열의 legth필드는 읽기만 가능, 값 변경 불가능
4. 배열 길이 for문 사용해서 전체 배열 항목 반복시 많이 사용

[다차원 배열]
1. 배열 항목에 또다른 배열 대입된 배열

[값 목록으로 다차원 배열 생성]
1. 값 목록으로 배열을 생성시 배열 변수 선언시 타입뒤에 []를 차원수 만큼 붙이기, 
   목록 또한 {} 중첩

[new연산자로 다차원 배열 생성]
배열변수 선언시 타입뒤에 []를 차원 수만틈 붙이고, new타입 뒤에서 차원수 만큼 [] 붙이기

[배열에서 객체 참조]
1. 기본타입 배열 - 각 항목의 값을 직접 저장
2. 참조 타입(class, interface) - 각 항목에 객체의 번지 저장

[배열 복사하기]
1. 배열은 한 번 생성하면 길이 변경 X, 저장공간 더 필요시 더 큰 길이의 배열 새로 제작, 이전 배열로 부터 복사
2. system의 arraycopy()로 배열 복사 가능

[배열 및 컬레션 처리에 용이한 for문]
1. 카운터 변수, 증감식 사용 X, 항목 개수만큼 반복 후 자동으로 for문 탈출
2. for문 실행 - 배열에서 가져올 항목 존재 - 변수에 항목 저장, 실행문 실행
3. 다시반복 배열에서 가져올 다음 항목 존재 2-> 3-> 1 로 진행, 항목 X for문 종료

[String[] args] 매개변수의 필요성]
1. 자바 프로그램을 실행하기 위해 main()메소드 작성하면서 매개변수 필요
2. 실행시 입력값 부족하면 길이가 0인 String 배열 참조

[Enum타입]
1. 한정된 값을 갖는 타입(ex/ 계절)
2. 열거타입이름으로 소스파일 생성 - 한정 된 값 코드로 정의
3. 열거 타입 이름 - 첫문자 : 대문자 캐멀 스타일로 짓기
